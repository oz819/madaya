# تحليل المشروع الأصلي — quran_halaqat_tracker_tarbawi_v2-2.html

هذا التحليل مبني على قراءة كاملة لملف الـ prototype الأصلي (733 سطر) قبل كتابة أي كود جديد،
كما هو مطلوب. الهدف: توثيق كل شيء موجود، تحديد التكرار والتعارض، وتحديد ما ينتقل إلى Backend
مقابل ما يبقى في Frontend.

## 1. الميزات (Features) الموجودة في الملف الأصلي

### 1.1 تسجيل الدخول والمستخدمون
- شاشة دخول واحدة (username + password)، حساب تجريبي `admin/1234`.
- دورين: `manager` (مدير) و `teacher` (محفظ).
- المدير يرى كل الحلقات، المحفظ يرى فقط الحلقات المسندة إليه (`allowedCircles()`).

### 1.2 لوحة التحكم (Dashboard)
- إجمالي الطلاب، عدد الحاضرين اليوم، عدد من سمّعوا اليوم، عدد من يحتاجون متابعة (نقاط < 60).
- مؤشرات تربوية يومية (عدد ملاحظات إيجابية/سلبية اليوم، عدد الطلاب تحت 60).
- ترتيب الطلاب حسب موضع الحفظ (مسار من الفاتحة إلى الناس) لكل حلقة.
- ملخص تقدم التلاوة (Top 8 حسب نسبة الإنجاز من إجمالي القرآن).

### 1.3 الطلاب
- جدول بكل الطلاب المرئيين للمستخدم الحالي، مع فلترة حسب الحلقة وبحث بالاسم.
- الأعمدة: الاسم، الحلقة، المحفظ، آخر حفظ، الرصيد (نقاط)، التقييم النصي.

### 1.4 التقييم اليومي
- نموذج: الحلقة، الطالب، الحضور (حاضر/متأخر/غائب بعذر/غائب بلا عذر)،
  التسميع (لم يسمع/ممتاز/جيد جدًا/جيد/إعادة/تمكين)، نوع العمل (حفظ جديد/مراجعة/تلاوة)،
  السورة، من آية، إلى آية، نقاط إيجابية إضافية يدوية (0..+3)، نقاط سلبية إضافية يدوية (0..-4)،
  ملاحظة نصية حرة.
- عند الحفظ: يُحسب `points` تلقائيًا عبر `basePoints()` ثم يُضاف إليه التعديل اليدوي، ويُخزَّن
  السجل في `quran_daily`. إن كان نوع العمل "حفظ جديد" وتم إدخال سورة، يُحدَّث موضع الطالب الحالي
  (`surah/from/to/grade/surahNo`) مباشرة على كائن الطالب.
- سلم نقاط معتمد موثّق نصيًا في الواجهة (حضور +2، في الوقت +1، تمكين +5، ممتاز +4، جيد جدًا +3،
  جيد +2، مراجعة متقنة +3، تلاوة +1، تحسن +2، مبادرة حتى +3 / لم يسمع بلا عذر −3، إعادة −2،
  غياب بلا عذر −3، تأخر −1، عدم واجب المراجعة −2، تقصير متكرر حتى −3، سلوك مؤثر حتى −4).
  **ملاحظة:** منطق `basePoints()` الفعلي في الكود لا يطابق هذا الجدول بشكل كامل (مثلاً لا يوجد
  خصم "تأخر −1" أو "عدم واجب المراجعة −2" في الكود الفعلي) — هذا تعارض بين التوثيق النصي
  والتنفيذ البرمجي يجب حسمه (نتبع الكود كمصدر حقيقة، ونوثق الفروق).

### 1.5 متابعة التلاوة
- مسار مستقل بالكامل عن الحفظ: من الفاتحة إلى الناس، بالاعتماد على قائمة `quran` (114 سورة
  مع عدد آياتها) لحساب index تراكمي للآيات ونسبة الإنجاز (`tilIndex`, `tilPct`).
- نموذج: الحلقة، الطالب، السورة (dropdown يبني `max` تلقائيًا لعدد الآيات)، من آية، إلى آية،
  تقييم، ملاحظات. يُخزَّن في `quran_tilawah`.
- عرض: آخر موضع تلاوة لكل طالب، مرتّب تنازليًا حسب التقدم، مع progress bar بالنسبة المئوية.

### 1.6 المتابعة التربوية
- نموذج: الحلقة، الطالب، المجال (المواظبة/الجدية/الإتقان/أدب الحلقة/الأثر والمبادرة)،
  إجراء بنقاط ثابتة (+2/+1/−1/−2/−3)، ملاحظة نصية (إلزامية).
- يُخزَّن في `quran_edu`. يُعرض آخر 30 ملاحظة (كل الحلقات المرئية) مرتبة بالتاريخ تنازليًا.

### 1.7 نظام النقاط والرصيد الشهري (`scoreForStudent`)
- **صيغة الاحتساب الفعلية:** `50 + Σ(quran_daily.points لنفس الشهر) + Σ(quran_edu.points لنفس الشهر)`،
  محصور بين 0 و100. **لا يشمل** سجلات `quran_tilawah` (التلاوة لا تدخل في الرصيد التربوي إطلاقًا).
- تصنيف نصّي: ≥90 متميز، ≥80 متقدم، ≥70 جيد، ≥60 يحتاج تعزيزًا، وإلا يحتاج متابعة.
- **تعارض مكرر:** يوجد نظام "نقاط سريعة" منفصل تمامًا (`points-tarbawi-js`، مفتاح
  `quranTrackerPointsV4`, دوال `addPoint/openPointsPanel/monthlyTarbiyaReport`) يحسب رصيدًا
  آخر تمامًا بصيغة مختلفة (`getTarbiyaLevel` بلا الأساس 50) — **هذا النظام غير مستخدم فعليًا في
  أي مكان من الواجهة** (لا يوجد أي زر `onclick="openPointsPanel(...)"` في الـ HTML المعروض،
  فهو كود ميت/orphaned من نسخة سابقة). القرار: **إسقاطه بالكامل** والاعتماد فقط على
  `scoreForStudent` الموجود فعليًا ضمن التدفق الأساسي (راجع القسم 4).

### 1.8 التقارير (واتساب)
- نص جاهز للنسخ (`renderReport` + `copyReport` عبر Clipboard API) يلخّص لكل طالب في حلقة معيّنة
  (أو كل الحلقات): آخر سجل يومي لهذا اليوم (النوع، الموضع، التقييم، النقاط) + الرصيد الشهري.
- توليد نصي بحت من بيانات موجودة أصلًا — لا يحتاج تخزينًا خاصًا به.

### 1.9 الإدارة (Manage) — المسار الأساسي المستخدم فعليًا
- إضافة محفظ (اسم/username/password) → دور `teacher` تلقائيًا.
- إضافة حلقة (اسم + اختيار محفظ واحد `teacherId`).
- إضافة طالب (اسم + اختيار حلقة).
- جداول عرض للمحفظين والحلقات (مع عدد الطلاب لكل حلقة).
- هذا القسم مرئي فقط لدور `manager` (`manageNav`).

### 1.10 لوحة إدارة أخرى (Modal) — نظام مكرر/غير مفعّل
- زر "⚙️ الإدارة" الأول في شريط التنقل ينادي `showAdminPanel()` من سكربت منفصل
  (`multi-user-admin-js`) يعتمد على مفاتيح تخزين مختلفة كليًا: `quranTrackerUsersV3`,
  `quranTrackerCirclesV3`, `quranTrackerAssignmentsV3`, و`quranCurrentUser`.
- **عطل حرج فعلي:** `showAdminPanel()` يتحقق من `getCurrentUser()` التي تقرأ `quranCurrentUser`
  — لكن `login()` الأساسي (المستخدَم فعليًا عند الدخول) **لا يكتب أبدًا** هذا المفتاح، فقط
  `loginQuranUser()` (غير المستخدَمة من نموذج الدخول الفعلي) تكتبه. النتيجة: **زر "⚙️ الإدارة"
  الأول يفشل دائمًا** ("هذه الصفحة متاحة للمدير فقط") بغض النظر عن هوية المستخدم المسجّل فعليًا.
- يدعم هذا النظام الموازي: تعديل/حذف محفظين، تعديل/حذف حلقات، ربط حلقة بعدة محفظين
  (`teacherIds[]` — علاقة many-to-many)، وتوزيع الطلاب على الحلقات عبر checkboxes
  (`toggleStudentCircle`) — لكنه يعمل على مصفوفة طلاب منفصلة (`window.students` أو
  `localStorage['students']`) لا علاقة لها بمصفوفة `students` الحقيقية المستخدمة في بقية
  التطبيق. **قرار التوحيد:** هذا القسم بالكامل (Modal + سكربتاه المرتبطان `circleFilterBar`
  و`initCircleFilter`) **كود ميت غير متصل بالتدفق الفعلي** ويُحذف عند إعادة البناء؛ وظائفه
  المفيدة (تعديل/حذف حلقة ومحفظ) تُبنى من جديد ضمن قسم "الإدارة" الموحّد الوحيد (1.9) في الـ API
  الجديد، مع دعم CRUD كامل هذه المرة (الأصل يدعم فقط الإضافة، بلا تعديل أو حذف أو تعطيل).

## 2. كل مفتاح LocalStorage مستخدم فعليًا

| المفتاح | البيانات | يُستخدم فعليًا؟ |
|---|---|---|
| `quran_users` | مصفوفة المستخدمين (id, name, username, password نص صريح, role) | ✅ نعم — أساس تسجيل الدخول |
| `quran_circles` | مصفوفة الحلقات (id, name, teacherId مفرد) | ✅ نعم |
| `quran_students` | مصفوفة الطلاب (id, name, circleId, surah, surahNo, from, to, grade, review, tilawah) | ✅ نعم |
| `quran_daily` | سجلات التقييم اليومي | ✅ نعم |
| `quran_edu` | ملاحظات النمو التربوي | ✅ نعم |
| `quran_tilawah` | سجلات متابعة التلاوة | ✅ نعم |
| `quranTrackerUsersV3` | مستخدمون بنظام موازٍ (يشمل `active`) | ❌ كود ميت (لوحة الإدارة المعطّلة) |
| `quranTrackerCirclesV3` | حلقات بنظام موازٍ (`teacherIds[]`) | ❌ كود ميت |
| `quranTrackerAssignmentsV3` | ربط studentId→circleId بنظام موازٍ | ❌ كود ميت |
| `quranCurrentUser` | جلسة المستخدم الحالي لنظام الإدارة الموازي | ❌ كود ميت (لا يُكتب من `login()` الفعلية) |
| `quranTrackerPointsV4` | سجل نقاط منفصل عن `quran_daily`/`quran_edu` | ❌ كود ميت (لا يوجد زر يفتحه) |
| `quranTrackerGradesV4` | معرّف فقط، غير مستخدم فعليًا في أي دالة | ❌ غير مستخدم إطلاقًا |

**خلاصة مهمة جدًا:** كل كلمات المرور في هذا التطبيق مخزّنة **نص صريح (plain text)** —
`password:"1234"` — وتُقارَن مباشرة `x.password===p`. هذا غير مقبول لأي نظام Production
ويجب استبداله بالكامل بـ hashing (bcrypt/argon2) من أول يوم في الـ Backend، وعدم إرسال/تخزين
كلمة المرور نصًا صريحًا في أي مكان.

## 3. شكل البيانات والعلاقات

```
User (id, name, username, passwordHash, role[ADMIN|TEACHER], active)
  └─ 1:N → Circle (teacherId FK اختياري)

Circle (id, name, teacherId FK → User)
  └─ 1:N → Student (circleId FK)

Student (id, name, circleId FK,
         currentSurahNo, currentSurah, currentFromAyah, currentToAyah, currentGrade)
  ├─ 1:N → DailyRecord (attendance, grade, workType, surah, fromAyah, toAyah, points, notes, date, teacherId)
  ├─ 1:N → TilawahRecord (surahNo, surah, fromAyah, toAyah, grade, notes, date, teacherId)
  └─ 1:N → EduNote (area, points, note, date, teacherId)
```

- كل السجلات (Daily/Tilawah/Edu) ترتبط أيضًا بـ `teacherId` (من سجّل الحركة) لأغراض التتبع
  والتدقيق (audit)، وهذا غير موجود صراحة كحقل منفصل في الأصل لكنه محسوب ضمنيًا
  (`teacherId: currentUser.id`) — نُبقيه صراحةً في الـ schema.
- قائمة السور الـ 114 (اسم + عدد آيات) بيانات مرجعية ثابتة (reference data) لا تتغيّر، تبقى
  ملف JSON ثابت مشترك بين Frontend و Backend (وليست جدول DB يحتاج CRUD).

## 4. المشاكل والتكرار والتعارضات المكتشفة (بالترتيب حسب الخطورة)

1. **كلمات مرور نص صريح** — خطر أمني حرج. (معالجة: hashing + عدم إعادة إرسال الباسورد في أي API response).
2. **الصلاحيات مطبّقة في الـ Frontend فقط** (`allowedCircles()`، إخفاء زر الإدارة بـ CSS) — أي
   مستخدم يقدر يتلاعب بـ localStorage أو الـ DOM ويشوف بيانات كل الحلقات. (معالجة: كل تحقق
   صلاحية ينتقل للـ Backend، الـ Frontend يعرض فقط ما يرجعه الـ API).
3. **نظامان متوازيان كاملان لتسجيل الدخول والإدارة وحتى النقاط** لا يتصلان ببعض إطلاقًا، أحدهما
   ميت فعليًا لكنه يحمّل الصفحة أكواد وحقول تخزين لا فائدة منها ويسبب لبس (زر "⚙️ الإدارة" يبدو
   للمستخدم أنه يعمل لكنه دائمًا يفشل). (معالجة: حذف النظام الميت بالكامل، توحيد كل شيء API واحد).
4. **تعارض توثيق سلّم النقاط مقابل تنفيذه الفعلي** في `basePoints()` (قسم 1.4). (معالجة: نعتمد
   السلوك الفعلي في الكود كمصدر حقيقة وننقله للـ Backend بنفس المنطق تمامًا حتى لا نغيّر سلوك
   محسوب عليه المستخدمون حاليًا، مع توثيق الفرق هنا).
5. **لا يوجد أي تحقق من صحة المدخلات** (مثال: `saveEdu` يسمح بحفظ نقاط سالبة/موجبة عشوائية بلا
   قيود، `saveTilawah` يتحقق فقط أن `to>=from` بدون التأكد أن `to` لا يتجاوز عدد آيات السورة).
   (معالجة: Zod schemas صارمة في الـ Backend لكل Endpoint).
6. **لا صفحة/تحديث "تعديل" أو "حذف" لأي كيان** (محفظ/حلقة/طالب) في المسار الفعلي المستخدم —
   فقط إضافة. الأصل لا يدعم تعطيل محفظ أو نقل طالب بين حلقات من الواجهة الفعلية رغم أن الميزة
   الميتة كانت تحاول توفير هذا. (معالجة: نبنيها بشكل صحيح هذه المرة في الـ API + الواجهة).
7. **حساب الرصيد الشهري (`scoreForStudent`) يُنفَّذ بالكامل على الـ Frontend بحلقات تكرارية على
   كل السجلات** — غير قابل للتوسع مع بيانات حقيقية كبيرة. (معالجة: يُحسب بالـ Backend عبر
   استعلام SQL/Prisma مجمّع `GROUP BY`، ويُعاد جاهزًا ضمن استجابة API).
8. **لا Audit trail حقيقي وشفاف** لمن عدّل ماذا ومتى (فقط `teacherId` أحيانًا). (معالجة: كل
   الجداول تحصل على `createdAt`, وسجلات لا تُحذف/تُعدَّل بعد إدخالها من قبل غير الأدمن، فقط تُضاف
   — matches الأصل: لا يوجد أصلًا زر تعديل/حذف لسجل يومي أو تلاوة أو ملاحظة تربوية بعد إدخاله).

## 5. ماذا ينتقل إلى Backend

- كل بيانات النطاق (Users, Circles, Students, DailyRecord, TilawahRecord, EduNote) → PostgreSQL
  عبر Prisma، خلف REST API.
- كل منطق حساب النقاط (`basePoints`, `scoreForStudent`, `scoreLabel`) → دوال Backend مشتركة
  تُستدعى عند الحفظ (لتخزين `points` النهائية) وعند القراءة (لحساب الرصيد التجميعي).
- كل منطق التحقق من الصلاحيات (`allowedCircles`, `visibleStudents`) → middleware/queries تُفلتر
  حسب `req.user` في كل Endpoint (وليس فقط إخفاء بالواجهة).
- Hashing لكلمات المرور، إصدار جلسة/JWT، التحقق من الجلسة.
- توليد نص التقرير (`renderReport`) يمكن إبقاؤه في الـ Frontend (تجميع نصي بسيط من بيانات أتت
  أصلًا من الـ API لهذا اليوم) — لا حاجة Endpoint خاص به، لكن يُفضَّل أيضًا نقله للـ Backend
  كـ Endpoint `GET /circles/:id/report` ليبقى مصدر الحقيقة واحدًا ولإتاحة استخدامه لاحقًا من
  قنوات أخرى (بوت واتساب مثلاً) — **قرار:** يبقى بسيطًا في الـ Frontend حاليًا (كما طلب عدم
  المبالغة)، مبني على بيانات الـ API لهذا اليوم فقط.

## 6. ماذا يبقى في Frontend

- كامل الـ HTML/CSS الحالي (الشكل والتصميم لا يتغيّر إلا للضرورة التقنية: فصل الملف الواحد إلى
  `index.html` + `styles.css` + ملفات JS منظمة بدل inline scripts متعددة ومتضاربة).
- منطق بناء عناصر الواجهة (dropdowns، جداول، progress bars) من بيانات الـ API.
- قائمة السور الـ 114 الثابتة (`quran` array) — مرجع عرض فقط، تُستنسخ أيضًا بالـ Backend
  للتحقق من صحة أرقام الآيات عند الحفظ (لا يمكن الوثوق بأرقام تأتي من المتصفح فقط).
- تفضيلات واجهة بسيطة جدًا فقط إن لزم (مثل آخر تبويب مفتوح) عبر `localStorage` — وليس أي بيانات نطاق.
- توليد نص تقرير واتساب والنسخ للحافظة (`navigator.clipboard`).

## 7. القرار التصميمي النهائي للنظام الموحّد

- دور واحد فقط لكل مستخدم: `ADMIN` أو `TEACHER` (نفس تسمية الأصل `manager/teacher` لكن بأسماء
  enum إنجليزية في الـ DB، وتُعرض عربيًا في الواجهة كما هي).
- حلقة واحدة لها محفظ رئيسي واحد فقط (`teacherId` اختياري) — **لا** علاقة many-to-many كما في
  النظام الميت، لأن كل بقية الكود الفعلي (العرض، النقاط، التقارير) مبني على افتراض محفظ واحد
  لكل حلقة؛ توسيعها لاحقًا ممكن دون كسر شيء إن احتاج المشروع ذلك مستقبلًا.
  نُتيح مع ذلك للأدمن نقل حلقة من محفظ لآخر (تعديل `teacherId`).
- نظام نقاط واحد فقط: `DailyRecord.points` + `EduNote.points`، بلا أي "سجل نقاط سريع" منفصل.
