# رسائل المصادقة (تأكيد الحساب + إعادة تعيين كلمة المرور)

## التشخيص: لماذا لم تكن الرسائل تصل

فُحصت سجلات Supabase Auth (`auth_logs`) فعليًا، والنتيجة:

**إيميل واحد فقط نجح إرساله في تاريخ المشروع بأكمله:**
```
mail.send → mail_to: osamazaedsefalden+admin@gmail.com
            mail_from: noreply@mail.app.supabase.io
            mail_type: confirmation          (2026-09-12T16:26:16Z)
```
وكل طلب بعده فشل بنفس الخطأ:
```
429  over_email_send_rate_limit   "email rate limit exceeded"
```
(طلبا تأكيد حساب + طلب إعادة تعيين كلمة مرور — كلها رُفضت.)

`mail_from: noreply@mail.app.supabase.io` يثبت أن المشروع يستخدم **خدمة البريد المدمجة الافتراضية
في Supabase، ولا يوجد SMTP خاص**. وهذه الخدمة — بحسب توثيق Supabase الرسمي — فيها قيدان يجعلانها
غير صالحة لهذا التطبيق:

1. **الحد: إيميلان فقط في الساعة**، والتسليم "best-effort"، وهي حرفيًا *"for demonstration
   purposes only… not meant for production"*.
2. **الأهم: ترسل فقط إلى عناوين أعضاء منظمة Supabase.** أي عنوان آخر يفشل بـ
   *"Email address not authorized"*. أي أن إضافة محفظ ببريده الشخصي **لن تصله رسالة أبدًا**،
   حتى لو كان الحد مرتفعًا.

### ملاحظة مهمة: التطبيق نفسه لم يكن يرسل أي رسالة أصلًا

بفحص الكود، لا يوجد أي استدعاء لـ `signUp()` ولا `resetPasswordForEmail()` في النسخة السابقة:
- إضافة محفظ تستخدم `auth.admin.createUser({ email_confirm: true })` — تُنشئ الحساب **مؤكَّدًا
  مباشرة، بلا أي رسالة** (سلوك مقصود في التصميم السابق).
- إعادة تعيين كلمة المرور تستخدم `auth.admin.updateUserById({ password })` — المدير يضبطها
  مباشرة، **بلا أي رسالة**.

فالرسائل الفاشلة في السجلات كانت من استدعاءات يدوية وقت الإعداد الأولي للمشروع، لا من التطبيق.

## ما تم بناؤه في التطبيق (جاهز ومُختبر)

| المسار | الوظيفة |
|---|---|
| `app/forgot-password/page.tsx` | إدخال البريد → `resetPasswordForEmail()` |
| `app/auth/confirm/route.ts` | نقطة وصول كل روابط الإيميل → تُنشئ الجلسة ثم تحوّل للصفحة المطلوبة |
| `app/reset-password/page.tsx` | تعيين كلمة مرور جديدة → `updateUser({ password })` |
| رابط "نسيت كلمة المرور؟" | أُضيف أسفل نموذج تسجيل الدخول |

**التحقق الفعلي الذي أُجري:** استُدعي `/auth/confirm` برمز وهمي، فوصل الطلب إلى `verifyOtp` في
Supabase ورُدّ بشكل صحيح بـ *"Email link is invalid or has expired"* — أي أن السلسلة كاملة
موصولة وتعمل، والناقص فقط رمز حقيقي يأتي عبر بريد فعلي.

**ملاحظة تقنية مقصودة:** استُخدم نمط `token_hash` + `verifyOtp` وليس `code` + PKCE، لأن نمط
`token_hash` يعمل **حتى لو فتح تطبيق البريد الرابط في متصفح مختلف** عن الذي طلبه — وهذه هي
الحالة الشائعة على الهاتف. (الكود يقبل النمطين احتياطًا.)

---

# الخطوات المطلوبة منك (لا يمكن إنجازها من الكود)

## 1. إعداد SMTP خاص

اختر مزوّدًا. الأنسب لحجم هذا المشروع (خطط مجانية كافية):

| المزوّد | ملاحظة |
|---|---|
| **Resend** | الأسهل مع Supabase، وله دليل تكامل رسمي |
| **Brevo** (سابقًا Sendinblue) | خطة مجانية سخية |
| **AWS SES** | الأرخص على نطاق واسع، لكن إعداده أطول |

**يُفضَّل بشدة أن يكون لديك دومين خاص** وتوثّقه عند المزوّد (سجلات SPF/DKIM). الإرسال من دومين
موثّق هو الفرق بين وصول الرسالة إلى البريد الوارد ووصولها إلى مجلد الرسائل غير المرغوبة.

ثم في Supabase Dashboard → **Project Settings → Authentication → SMTP Settings** → فعّل
*Enable Custom SMTP* واملأ القيم التي يعطيك إياها المزوّد:

```
Host            (مثال: smtp.resend.com)
Port            (عادة 465 أو 587)
Username
Password        ← سرّي: ضعه في اللوحة مباشرة، لا تشاركه في أي محادثة أو ملف
Sender email    (مثال: no-reply@your-domain.com)
Sender name     حلقات القرآن
```

بعد التفعيل يصبح الحد الافتراضي **30 مستخدمًا جديدًا في الساعة** (قابل للتعديل من
Authentication → Rate Limits).

## 2. ضبط Site URL و Redirect URLs

Supabase Dashboard → **Authentication → URL Configuration**:

- **Site URL**: رابط التطبيق النهائي بعد النشر (مثال: `https://your-app.vercel.app`).
  هذا ما سيُبنى عليه رابط كل رسالة، فلا يصح أن يبقى `localhost`.
- **Redirect URLs**: أضف الأنماط التالية:
  ```
  https://your-app.vercel.app/**
  ```
  وللاختبار المحلي/الهاتف على الشبكة المحلية أضف أيضًا (مؤقتًا):
  ```
  http://localhost:3000/**
  http://192.168.100.9:3000/**
  ```

## 3. تعديل قوالب الرسائل (خطوة إلزامية)

Supabase Dashboard → **Authentication → Email Templates**. القوالب الافتراضية تستخدم
`{{ .ConfirmationURL }}`، وهو غير موثوق على الهاتف. استبدل المحتوى بالتالي:

**قالب Reset Password:**
```html
<h2>إعادة تعيين كلمة المرور</h2>
<p>لتعيين كلمة مرور جديدة لحسابك في منظومة متابعة حلقات القرآن، اضغط الرابط التالي:</p>
<p>
  <a href="{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=recovery&next=/reset-password">
    تعيين كلمة مرور جديدة
  </a>
</p>
<p>إذا لم تطلب ذلك، يمكنك تجاهل هذه الرسالة.</p>
```

**قالب Confirm signup:**
```html
<h2>تأكيد الحساب</h2>
<p>لتأكيد بريدك الإلكتروني والدخول إلى منظومة متابعة حلقات القرآن، اضغط الرابط التالي:</p>
<p>
  <a href="{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=signup&next=/">
    تأكيد الحساب
  </a>
</p>
```

**قالب Invite user** (إن أردت لاحقًا دعوة المحفظين ليضبطوا كلمات مرورهم بأنفسهم):
```html
<h2>دعوة للانضمام</h2>
<p>تمت إضافتك كمحفظ في منظومة متابعة حلقات القرآن. اضغط الرابط لتعيين كلمة مرورك:</p>
<p>
  <a href="{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=invite&next=/reset-password">
    تعيين كلمة المرور والدخول
  </a>
</p>
```

## 4. الاختبار بعد الإعداد

1. من صفحة الدخول اضغط **"نسيت كلمة المرور؟"** وأدخل بريدًا حقيقيًا لحساب موجود.
2. يجب أن تصل الرسالة خلال ثوانٍ. لو لم تصل: راجع سجلات المزوّد أولًا، ثم
   Supabase Dashboard → Logs → Auth Logs وابحث عن `mail.send` أو عن خطأ بدلًا منه.
3. اضغط الرابط → يجب أن ينقلك إلى صفحة تعيين كلمة مرور جديدة → اضبطها → تأكد من الدخول بها.

بعد نجاح ذلك، أخبرني لأزيل سجلات التشخيص المؤقتة (`/api/debug-log` و `lib/debugLog.ts`
و `[mw-debug]`) قبل النسخة النهائية.

## قرار مؤجَّل: هل يؤكد المحفظون بريدهم؟

حاليًا "إضافة محفظ" تُنشئ الحساب مؤكَّدًا مباشرة ولا ترسل رسالة (أقل احتكاكًا: المدير يسلّم
كلمة المرور للمحفظ). بعد تشغيل SMTP يمكن التحويل إلى **دعوة بالبريد** (`inviteUserByEmail`)
بحيث يضبط كل محفظ كلمة مروره بنفسه ولا يعرفها المدير إطلاقًا — وهو الأفضل أمنيًا.
أخبرني إن أردت ذلك، فهو تغيير صغير في `app/api/admin/teachers/route.ts`.
